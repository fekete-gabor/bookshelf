import styled from "styled-components";

const ResetPassword = () => {
  return (
    <Wrapper>
      <h2>ResetPassword</h2>
    </Wrapper>
  );
};

const Wrapper = styled.section``;

export default ResetPassword;
