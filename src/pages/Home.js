import styled from "styled-components";

const Home = () => {
  return (
    <Wrapper>
      return <h2>home</h2>
    </Wrapper>
  );
};

const Wrapper = styled.section`
  height: 100vh;
`;

export default Home;
