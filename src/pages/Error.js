import styled from "styled-components";

const ErrorPage = () => {
  return (
    <Wrapper>
      <h2>ErrorPage</h2>
    </Wrapper>
  );
};

const Wrapper = styled.section``;

export default ErrorPage;
